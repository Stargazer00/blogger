<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <h1><%= @page.title %></h1>
</p>

<p>
  <strong>Body:</strong>
  <p><%= @page.body %></p>
</p>

<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Back', pages_path %>
